import Button from "../Button";
import { ListContainer } from "./style";

const List = () => {
  return (
    <ListContainer>
      LIST
      <Button background={"red"}>Add</Button>
    </ListContainer>
  );
};

export default List;
